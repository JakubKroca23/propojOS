---
phase: 1
plan: 2
wave: 2
---

# Plan 1.2: React/Vite Scaffolding

## Objective
Scaffold the React/Vite application with Vanilla CSS.

## Context
- .gsd/SPEC.md
- .gsd/DECISIONS.md

## Tasks

<task type="auto">
  <name>Scaffold React + Vite</name>
  <files>package.json, vite.config.ts, index.html, src/main.tsx, src/App.tsx</files>
  <action>
    Run `npm create vite@latest . -- --template react-ts` in the project root.
    - DO NOT overwrite existing files (like docker-compose.yml or .gsd), only initialize the Vite project.
    - Run `npm install` after scaffolding.
  </action>
  <verify>test -f package.json && test -f vite.config.ts</verify>
  <done>Vite React TypeScript project is scaffolded and dependencies are installed.</done>
</task>

<task type="auto">
  <name>Setup Vanilla CSS Architecture</name>
  <files>src/index.css, src/App.css, src/App.tsx</files>
  <action>
    Clean up the default Vite boilerplate.
    - Rewrite `src/index.css` to define core CSS variables for the theme (e.g., background colors, text colors, grid gaps).
    - Remove `src/App.css` and its import from `src/App.tsx`.
    - Update `src/App.tsx` to render a basic "Welcome to PropojOS" header using semantic HTML and Vanilla CSS classes.
  </action>
  <verify>grep "Welcome to PropojOS" src/App.tsx</verify>
  <done>Vite boilerplate is removed, Vanilla CSS is set up, and App.tsx renders the welcome message.</done>
</task>

## Success Criteria
- [ ] React/Vite app is scaffolded.
- [ ] Vanilla CSS structure is established.

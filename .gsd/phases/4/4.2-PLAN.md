---
phase: 4
plan: 2
wave: 2
---

# Plan 4.2: Visual Workflow Editor

## Objective
Integrate `React Flow` to build the node-based canvas for visually connecting apps.

## Context
- .gsd/SPEC.md
- src/components/Shell.tsx

## Tasks

<task type="auto">
  <name>Install React Flow</name>
  <files>package.json</files>
  <action>
    - Install `@xyflow/react`.
  </action>
  <verify>npm ls @xyflow/react</verify>
  <done>React Flow is installed.</done>
</task>

<task type="auto">
  <name>Build Workflow Canvas</name>
  <files>src/components/WorkflowEditor.tsx, src/App.tsx, src/components/Shell.tsx</files>
  <action>
    - Create `src/components/WorkflowEditor.tsx` implementing the React Flow canvas.
    - The nodes should automatically sync with the `widgets` array in `osStore.ts`. If a widget exists on the dashboard, it appears as a node.
    - Add a button in the `Shell.tsx` taskbar to toggle between the Dashboard View and the Workflow Editor View.
  </action>
  <verify>grep "@xyflow/react" src/components/WorkflowEditor.tsx</verify>
  <done>User can toggle to a canvas displaying their widgets as nodes.</done>
</task>

## Success Criteria
- [ ] `@xyflow/react` is rendering correctly.
- [ ] Dashboard widgets automatically appear as nodes on the canvas.

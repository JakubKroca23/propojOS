---
phase: 4
plan: 3
wave: 3
---

# Plan 4.3: Real-Time Piping Integration

## Objective
Connect the visual React Flow graph to the logical `eventStore` so drawing a line literally pipes data.

## Context
- .gsd/SPEC.md
- src/components/WorkflowEditor.tsx
- src/store/eventStore.ts

## Tasks

<task type="auto">
  <name>Sync Graph Edges to Event Store</name>
  <files>src/components/WorkflowEditor.tsx</files>
  <action>
    - When the user connects two nodes in `WorkflowEditor.tsx`, update the `connections` state in `eventStore.ts`.
    - When a connection is removed, remove it from the `eventStore.ts`.
  </action>
  <verify>grep "eventStore" src/components/WorkflowEditor.tsx</verify>
  <done>Visual edges map directly to logical data pipes.</done>
</task>

<task type="auto">
  <name>Create Demo Connected Apps</name>
  <files>src/components/widgets/SenderWidget.tsx, src/components/widgets/ReceiverWidget.tsx, src/store/osStore.ts</files>
  <action>
    - Create a `SenderWidget` that has a button to emit a random number to an `out` port.
    - Create a `ReceiverWidget` that listens on an `in` port and displays the number.
    - Register them in `osStore.ts` as default widgets to demonstrate the piping.
  </action>
  <verify>test -f src/components/widgets/SenderWidget.tsx</verify>
  <done>Two demo widgets prove that data flows across the visual pipes.</done>
</task>

## Success Criteria
- [ ] Drawing a connection between the Sender and Receiver causes the Receiver to update when the Sender emits data.

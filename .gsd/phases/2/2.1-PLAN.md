---
phase: 2
plan: 1
wave: 1
---

# Plan 2.1: State Management & Shell Architecture

## Objective
Set up Zustand for global OS state and build the main Shell wrapper.

## Context
- .gsd/SPEC.md
- .gsd/DECISIONS.md

## Tasks

<task type="auto">
  <name>Setup Zustand Store</name>
  <files>src/store/osStore.ts</files>
  <action>
    - Install `zustand`.
    - Create `src/store/osStore.ts` exporting a `useOsStore` hook.
    - The state should hold an array of `widgets` (id, title, components) and `layouts` (for react-grid-layout).
    - Provide actions to `addWidget`, `removeWidget`, and `updateLayouts`.
  </action>
  <verify>test -f src/store/osStore.ts</verify>
  <done>Zustand store is created and exports the necessary state and actions.</done>
</task>

<task type="auto">
  <name>Build Shell Component</name>
  <files>src/components/Shell.tsx, src/components/Shell.css</files>
  <action>
    - Create `src/components/Shell.tsx` and its CSS.
    - The shell should feature a top Taskbar (or navigation header) with a "Sign Out" button.
    - It should render its `children` (which will be the Dashboard grid) in the main content area.
  </action>
  <verify>test -f src/components/Shell.tsx</verify>
  <done>Shell component is built with a taskbar and content slot.</done>
</task>

## Success Criteria
- [ ] Zustand store is fully functional.
- [ ] Shell component renders correctly with child slots.

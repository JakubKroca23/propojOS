---
phase: 2
plan: 2
wave: 2
---

# Plan 2.2: Dashboard Grid Integration

## Objective
Implement a draggable grid layout for apps/widgets using `react-grid-layout`.

## Context
- .gsd/SPEC.md
- src/store/osStore.ts

## Tasks

<task type="auto">
  <name>Build Dashboard Component</name>
  <files>src/components/Dashboard.tsx, src/components/Dashboard.css</files>
  <action>
    - Install `react-grid-layout`, `react-resizable` and their types.
    - Create `src/components/Dashboard.tsx`.
    - Implement a `Responsive, WidthProvider` grid from `react-grid-layout`.
    - Connect the grid to `useOsStore` to read/write layout changes.
    - Render dummy widgets to demonstrate drag/drop capabilities.
  </action>
  <verify>test -f src/components/Dashboard.tsx</verify>
  <done>Dashboard renders the grid layout using Zustand state.</done>
</task>

<task type="auto">
  <name>Integrate Shell & Dashboard into App</name>
  <files>src/App.tsx</files>
  <action>
    - Update `src/App.tsx` to remove the temporary Welcome message.
    - Render `<Shell>` and put `<Dashboard />` inside it when authenticated.
    - Move the sign-out logic from App.tsx into the Shell component.
  </action>
  <verify>grep "Dashboard" src/App.tsx</verify>
  <done>App.tsx successfully composes the Shell and Dashboard.</done>
</task>

## Success Criteria
- [ ] react-grid-layout is integrated and functional.
- [ ] App correctly renders the new layout structure.
